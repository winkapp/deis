/* Package portunes (poor-tune-ayes) provides a service for running tests at given intervals.

This provides both an HTTP/JSON API service for fetching test results and a
long-lived runtime which runs tests at given intervals.
*/
package portunes
